// A curated blurb, not a full review -- shorter than
// MAX_EDITORIAL_REVIEW_LENGTH (10000), since the point is a short editorial
// case for why this actor matters, not a comprehensive writeup.
export const MAX_PERSON_SPOTLIGHT_LENGTH = 1000;
